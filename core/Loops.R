# while-loop just like any other c-like prog. languange
i <- 0
while (i < 10) {
  print(i) # within a loop, we need to use the function print
  i <- i + 1
}

# for-loops in R are like for-each loops.
for (i in 1:10) {
  print(i)
}
